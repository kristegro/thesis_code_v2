[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "thesis-code"
version = "1.0.0"
description = ""
license = "Apache-2.0"
dependencies = [
    "flwr==1.20.0",
    "prometheus_client==0.24.1"
]

[tool.hatch.build.targets.wheel]
packages = ["."]